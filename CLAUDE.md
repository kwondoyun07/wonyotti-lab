# CLAUDE.md — wonyotti-lab

## 프로젝트
워뇨띠(aoa)의 BitMEX 체결·지갑내역(2018.03–2021.12)으로 (1) 행동 분석 (2) 모방 모델 (3) 그의 원칙 기반 리스크 엔진을 만든다.
대상: 코인, BTC 우선. 개인 연구용 — 이 데이터로 만든 봇·2차 제작물은 유료로 팔지 않는다(원작자 요청).

## 아키텍처
코드(숫자·날짜 계산, 구간화) → Jev(텍스트 즉답 판단) → 학습 모델(목표 노출 제안) → 리스크 엔진(최종 거부권) → 주문/관망

## 반드시 지킬 규칙
- 숫자 계산·날짜 비교·캔들 원자료는 Jev에 넣지 않는다. 코드에서 계산해 이름 붙인 구간이나 문장으로만 넘긴다 (jev-1.13은 수치·날짜·카운팅에 약함).
- Jev 질문은 하나에 판단 하나. 여러 요소는 질문을 나눠 한 요청에 묶고 코드에서 가중합한다.
- 학습 라벨은 계약 포지션이 아니라 달러 기준 순노출 비율(`src/inverse.py`). XBT 인버스에서 1배 숏 = 0, 포지션 없음 = 1.
- 검증은 시간순 분할(walk-forward)만. 무작위 분할·셔플 금지.
- 평가는 정확도 대신 노출 변경 시점 적중 + 모방 정책 손익. "직전 노출 유지" 베이스라인을 항상 함께 보고한다.
- 백테스트 입력에서 날짜·절대가격을 제거/정규화한다 (모델 사전지식 누출 방지).
- `risk_engine.evaluate()`는 모델 출력보다 우선한다. 규칙 수치는 `config/risk_rules.yaml`에서만 바꾼다.
- `data/` 아래 실제 데이터 파일(원본·가공본)은 커밋하지 않는다. 빈 `.gitkeep` 두 개만 예외. `.env`도 커밋하지 않고, API 키를 코드에 쓰지 않는다.
- 코드 변경 후 `pytest` 통과를 확인한다.

## 명령
- 테스트: `pytest`
- 구조 확인: `python -m src.inspect_data inspect <파일>`
- 변환: `python -m src.inspect_data convert <파일> <출력.parquet> [--sheet 시트]`
- 노출 타임라인: `python -m src.exposure [--limit N]`
- Jev: `python -m src.jev_layer --headline "..."` / `--note "..."`
- 세션 지시문: 첫 세션 `prompts/01_kickoff.md`, 이후 `prompts/resume.md`

## 진행 상황
- [x] 인버스 계약 수학, 파일 변환, 노출 계산, 리스크 엔진 + 테스트 28개
- [ ] 실제 공개 파일 inspect → `config/columns.yaml` 매핑 확정
- [ ] 노출 타임라인 생성, 커뮤니티 분석(지정가 비중, 보유시간 추이)과 대조
- [ ] 본인 공개 수치(입금 14.4 BTC, 실현손익 3,537 BTC, 포지션 약 3,200회, 주문 약 2.3만 회)와 대조 검증 (`src/validate.py`)
- [ ] 같은 기간 시장 데이터 확보 (비트멕스 종료로 아카이브 접근성 확인)
- [ ] 코드 특징 + 기준 모델 (walk-forward)
- [ ] Jev 특징 ablation, 확률 임계값 보정
- [ ] 페이퍼 트레이딩

## 참고
- TypeSafe 문서: https://docs.typesafe.ai (Jev 코드 작업 시 TypeSafe 스킬 사용)
- 1차 버전은 XBT 인버스만. ETHUSD(콴토) 등은 제외 목록으로만 출력.
